{
  "name": "beads-bridge",
  "version": "2.0.0",
  "description": "Claude skill that bridges Beads issue tracking with project management tools (GitHub Projects, Shortcut, etc.)",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "beads-bridge": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && npm run copy-frontend",
    "copy-frontend": "rm -rf dist/frontend && mkdir -p dist/frontend && rsync -a src/frontend/ dist/frontend/",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "lint": "eslint src tests",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\"",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "beads",
    "github-projects",
    "project-management",
    "claude-skill",
    "issue-tracking"
  ],
  "author": "mrdavidlaing",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^20.19.25",
    "@typescript-eslint/eslint-plugin": "^8.46.4",
    "@typescript-eslint/parser": "^8.46.4",
    "@vitest/coverage-v8": "^4.0.9",
    "eslint": "^9.39.1",
    "prettier": "^3.0.0",
    "typescript": "^5.3.0",
    "typescript-eslint": "^8.46.4",
    "vitest": "^4.0.9"
  },
  "dependencies": {
    "@shortcut/client": "^2.3.1",
    "@types/express": "^5.0.5",
    "commander": "^14.0.2",
    "express": "^5.1.0",
    "octokit": "^5.0.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
